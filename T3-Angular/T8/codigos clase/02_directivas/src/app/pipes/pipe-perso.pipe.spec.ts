import { PipePersoPipe } from './pipe-perso.pipe';

describe('PipePersoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePersoPipe();
    expect(pipe).toBeTruthy();
  });
});
